<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.healthtracker">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_descriptor"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CareProviderHome" />
        <activity android:name=".ViewPatients" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".SearchResults" />
        <activity android:name=".AddPatient" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".UserSettings" />
        <activity android:name=".ViewPatientsProblems" />
        <activity android:name=".CareProviderProblemView" />
        <activity android:name=".AddCareProviderComment" />
        <activity android:name=".ViewUsers" />
        <activity android:name=".PatientHome" />
        <activity android:name=".AddProblem" />
        <activity android:name=".ViewMyProblems" />
        <activity android:name=".EditProblem" />
        <activity android:name=".AddorEditRecord" />
        <activity android:name=".PatientProblemView" />
        <activity android:name=".SlideShow" />
        <activity android:name=".ViewAUser" />
    </application>

</manifest>